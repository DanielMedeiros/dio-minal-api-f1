version: "3.8"

services:
  f1-api:
    container_name: f1-minimal-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3333:3333"
    environment:
      - PORT=3333
      - NODE_ENV=production
    restart: unless-stopped
